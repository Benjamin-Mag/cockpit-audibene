# Chef d'orchestre — journal

Session : `projects-b4` (`72f84a`). Dossier : `Documents\Claude\Projects\Cockpit Audibene` (branche `main`).

## Responsabilités
- Relire et tester chaque PR ; merger sur `main` ; publier les versions (bump `public/manifest.json` + `npm pkg set version` + tag `vX.Y.Z`).
- Tenir `CLAUDE.md`, `README.md`, `docs/equipe/EQUIPE.md` et la mémoire projet à jour.
- Répondre aux questions des agents ; arbitrer ; remonter à Benjamin ce qui demande sa décision.

## État courant
- Version publiée : v1.0.30 (2026-09-14). Site : https://benjamin-mag.github.io/cockpit-audibene/
- `main` protégé (PR obligatoire) depuis le 2026-09-09.
- Chantiers ouverts : aucun. ORL Finder complet (étapes 1–3) ; retours de Benjamin attendus sur la v1.0.23 (appels Doctolib portés par l'onglet du site).

## Relecture d'une PR — check-list
1. `gh pr checkout <n>` dans un worktree de relecture, `npm ci`, `npx tsc --noEmit`, `npm run build`.
2. Charger `dist/` dans Edge, tester le parcours décrit dans la PR sur une vraie fiche.
3. Vérifier : pas de `confirm()`, pas d'appel réseau, `visibleEl` sur toute lecture Salesforce, données ajoutées gérées par `normalize`/`mergeData`, textes en français, animations douces.
4. Merge (`gh pr merge --squash`), puis publication si la fonctionnalité est utilisable par Benjamin.

## Journal
- 2026-09-08 : projet créé, étapes 1–4 livrées, v1.0.0 → v1.0.9.
- 2026-09-09 : peaufinage (v1.0.10 → v1.0.18), guide `CLAUDE.md`, mise en place de l'équipe et de l'Agent 1.
- 2026-09-09 : PR #2 (Agent 1, Partner Search étape 1) → v1.0.20. Benjamin change d'avis : Partner Search abandonné, remplacé par **ORL Finder** (prompt de Renaud Laurencin ; décisions : Doctolib d'abord, recherche en arrière-plan + lien « Ouvrir sur Doctolib », créneau visible seulement, rayon 20 km élargi si rien, pas de transfrontalier). Reconnaissance des API Doctolib faite par moi (voir mémoire projet).
- 2026-09-09 : PR #4 (ORL Finder étape 1) → v1.0.21 (permission cookies retirée, table codes postaux embarquée). PR #6 (étape 2 : téléphone, secteur, audiométrie, message type, TOP 3) → v1.0.22. Arbitrages : médecin sans numéro au lieu trouvé mais numéro ailleurs → affiché avec mention « autre cabinet » ; fiches praticien chargées : 20 max dans l'ordre du tri.
- 2026-09-09 : Benjamin teste la v1.0.22 : Doctolib bloque les appels partis du panneau (anti-robot, origine chrome-extension). Correctif préparé sur `fix/doctolib-via-onglet` (fetch exécuté dans l'onglet Doctolib en monde principal, `fetchViaTab`), intégré par l'Agent 1 dans sa PR #9 (étape 3 : filtres délai/secteur, rayon 20→40→60, cache 10 min, « autre cabinet », 20 fiches par paquets de 5, bouton « Voir l'onglet Doctolib »). Mergée → **v1.0.23**. Arbitrage : délai par défaut « tous » (14 j masquait les ORL sans RDV en ligne mais joignables), filtre délai côté API quand il est choisi.
- 2026-09-11 : demande de Benjamin : réordonner les textes du Chat partenaire → glisser-déposer des puces et des lignes de gestion, via un hook partagé `useDragReorder` + `moveById` (`components/drag.ts`) qui remplace aussi le code local de Mails. Au passage, bug d'accueil corrigé : « Sans dossier » restait bloqué à l'étape 1 sans modèle (`needsFolder`). Relecture adversariale par workflow (3 lentilles, 2 sceptiques par constat : 4 confirmés mineurs, corrigés ; 5 réfutés). Vérifié dans l'aperçu web (DnD simulé, ordre enregistré). → **v1.0.24**.
- 2026-09-13 : question de Benjamin sur le pilotage de Cockpit par le script d'Alice (projet local hors dépôt, Claude in Chrome + sfkit.js). Réponse : le panneau latéral est hors de portée d'une autre extension ; proposition d'une API dans la page Salesforce (désactivée par défaut, préparer sans envoyer, pas d'accès aux ventes, journal). Benjamin : « pas besoin de changer pour le moment ».
- 2026-09-14 : « Insérer dans Salesforce » coche le destinataire du composeur selon le modèle (patient → Client, partenaire → Partenaire), geste déjà validé en réel par Alice. Recherche limitée au composeur (éditeur + bouton Envoyer) après un test sur maquette où le « Client / Partenaire » du résumé d'appel voisin était coché. Bouton absent : mail inséré + message « coche à la main ». Incident de publication : une chaîne de commandes a poussé le tag v1.0.25 sur l'ancien code après l'échec d'une étape ; release et tag supprimés dans la minute, puis republiés. Leçon : publier avec `set -e`, jamais de `;` après une étape qui peut échouer. → **v1.0.25**.
- 2026-09-14 : test de Benjamin sur la v1.0.25 : au premier clic, « Partenaire introuvable » et corps vide ; au second clic, tout marche. Cause : juste après l'ouverture, Salesforce affiche l'éditeur avant les boutons Client / Partenaire, puis vide le corps en finissant son initialisation. Correctif : attente des boutons jusqu'à 6 s, pause après ouverture ou changement de destinataire, vérification que le corps est toujours là et réinsertion (2 essais). Testé sur maquette reproduisant ces délais. → **v1.0.26**.
- 2026-09-14 : retour de Benjamin : l'ORL Finder ne relançait rien quand il changeait le code postal ou l'adresse. (1) Le champ « Adresse du patient » pilote maintenant la recherche : code postal lu dans l'adresse après 900 ms sans frappe ou sur Entrée, gardé si l'adresse n'en contient plus, note quand il diffère de la fiche. (2) Le port de contexte relit le code postal de la Piste toutes les ~3 s (`codePostalEtVille`), et le panneau met la fiche à jour sans tout relire : une adresse modifiée et enregistrée dans Salesforce relance la recherche. Testé sur banc d'essai temporaire avec appels Doctolib interceptés : 5 cas OK. → **v1.0.27**.
- 2026-09-14 : **Salesforce figé** chez Benjamin après la v1.0.27. Cause probable : la relecture du code postal de la Piste toutes les ~3 s dans le port de contexte (`codePostalEtVille` → `fieldValue` → `deepAll` sur tout le shadow DOM Lightning, avec `chrome.dom.openOrClosedShadowRoot` par élément). Retirée : script de page, types et app.tsx remis à l'identique de la v1.0.26 ; la relance depuis l'adresse saisie dans Cockpit (panneau seulement) est conservée. Leçon : **aucune lecture `deepAll` périodique dans la page Salesforce** ; toute lecture régulière doit être ciblée ou déclenchée par un clic. → **v1.0.28**.
- 2026-09-14 : après la v1.0.28, « ça charge sans cesse ». Aucune attente du pont n'avait de limite (`sendMessage`, `executeScript`), et un onglet Doctolib mis en veille par Edge ne répond jamais. Correctif dans `bridge.ts` : `avecDelai` (ping 4 s, fiche 20 s, actions 90 s, appel Doctolib 25 s) ; onglets Doctolib gelés/déchargés ignorés, onglet muet écarté puis nouvel onglet en arrière-plan partagé entre appels simultanés ; plus aucun rechargement d'un onglet existant (formulaire possible) ; l'en-tête arrête ses relectures sur une page muette. Testé avec un faux navigateur (4 cas). → **v1.0.29**.
- 2026-09-14 : demandes de Benjamin : (1) chercher via Doctolib Pro (`https://pro.doctolib.fr/doctor_referrals_booking`) avec une indication si Cockpit n'y est pas connecté ; (2) limiter la liste à 15 ORL avec « Afficher plus ». Reconnaissance avec sa session (Claude in Chrome) : la page Pro utilise les mêmes API que le site public (`/patient-health-search/api/v1/hcp/search`, `/search/availabilities.json`, `/profiles/<slug>.json`) sur `pro.doctolib.fr`, mêmes résultats sur l'échantillon ; sans session, toute page Pro renvoie vers `auth.doctolib.fr/pro/…` ; la recherche Pro par URL exige un lieu de sa liste (`location=33510` = page d'erreur). Au passage : le lien public « Ouvrir sur Doctolib » ouvrait Marseille (`speciality=` et code postal seul ignorés) → `keyword=` + nom de ville. Implémenté : source Pro d'abord puis repli public (`ProDeconnecte`, `HorsSite` dans le pont), pause de 10 min avant de retenter Pro (pas d'onglet de connexion ouvert à chaque recherche), bandeau + bouton « Se connecter à Doctolib Pro », liens RDV sur le site de la source, 15 ORL puis +10 avec lecture des fiches à la demande. Tests : banc d'essai (32 faux ORL : 15 → 25 → 32) et faux navigateur (connecté, page de connexion ouverte, aucun onglet, second essai). → **v1.0.30**.
